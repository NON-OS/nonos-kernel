[package]
name = "nonos_app_skeleton"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "nonos_app_skeleton"
path = "src/lib.rs"

[dependencies]
nonos_libc = { package = "nonos_userland_libc", path = "../libc" }
