# Bootloader Hardening — Findings Register

Register seeded by W3 (Task 4): reachable panics, untrusted-input unwraps, swallowed errors, feature-gate proof, verify→use TOCTOU. W1 ABI findings live in `00-abi-equivalence-table.md`; W2 in `02-dedup-plan.md`; W5/W6 + full consolidation by Task 8. Only the LIVE boot spine was scored for reachability (see header of `02-dedup-plan.md` for the dead parallel trees deliberately excluded).

Live spine reused (verified, not blindly trusted): `efi_main` (main.rs:28) → `boot_entry` (entry/boot.rs:24) → `select_security_mode` (entry/mode.rs:21) → `run_security_checks` (boot/security/run.rs:28) → `initialize_zk_replay_protection` (boot/zk_init/init.rs:24) → `run_verified_boot` (entry/pipeline.rs:28) → `run_kernel_load`→`run_crypto_verification`→`run_zk_attestation`→`run_elf_parse`→`commit_rollback`→`run_handoff_prepare`.

Severity scale: S0 breaks boot/contract; S1 exploitable trust bypass; S2 weakened guarantee/silent fallback; S3 correctness/robustness; S4 cleanliness.

| ID | WS | Severity | path:line | Claim | Evidence | Recommended fix | Effort |
|----|----|----------|-----------|-------|----------|-----------------|--------|
| W3-01 | W3 | S1 | nonos-bootloader/src/menu/types/state.rs:22 | `SecurityMode::Development` is an ungated boot-menu entry, reachable on `production`/`hardened-production` builds, and selecting it skips kernel signature AND ZK verification. | `DEFAULT_ENTRIES` (state.rs:22) contains `MenuAction::Boot(SecurityMode::Development)` at index 8 with NO `#[cfg(feature)]`. Live reach: boot.rs:29 `select_security_mode` → mode.rs:27 (`dev_override`=false under non-`dev-mode` build, see W3-12) → `run_boot_menu` (menu/run.rs:25) → `KeyAction::Select` returns entry → action.rs:24 `MenuAction::Boot(mode) => Ok(mode)` → `SecurityMode::Development`. Then mode.rs:28 `requires_signature()` = `!matches!(Development)` = false, so signature/verify.rs:26 `handle_no_signature` / error.rs:37 `handle_invalid_signature` / no_proof.rs:26 / failed.rs:26 all take the dev branch (`continue`, no `fatal_reset`). Unsigned/unverified kernel boots. Compile-time `SecurityPolicy::from_build()` (security/enforce/policy/types.rs:21) does NOT constrain runtime `SecurityMode`; enforce_security_policy (security/enforce/core.rs:29) operates on firmware `SecurityContext`, never on the menu mode. Precondition: attacker has boot-console keyboard access within the 3 s timeout (default timeout → `MenuAction::Timeout` → `SecurityMode::Standard`, fail-safe). | Feature-gate the `Development` menu entry (and `resolve_action`) behind `#[cfg(feature="dev-mode")]` so production builds cannot select an unsigned mode. | M |
| W3-02 | W3 | S2 | nonos-bootloader/src/kernel_verify/verify.rs:40 | Kernel without a production footer skips signature verification entirely; enforcement then depends solely on runtime `SecurityMode`. | verify.rs:40 `if !has_production_footer(kernel_data)` sets `signature_present=false` and returns before `verify_and_display_signature` is ever called. Live reach: hash.rs:30 `verify_kernel_crypto` ← boot/crypto/hash.rs:30 ← run.rs:37. Downstream boot/crypto/signature/verify.rs:26 `!res.signature_present` → `handle_no_signature` which only `fatal_reset`s when `mode.requires_signature()` (non-Development). Fail-closed for Standard/Hardened, but the trust decision is centralized in the same single boolean exploited by W3-01; an attacker who can also reach Development (W3-01) gets unsigned boot with no footer. | Treat absent footer as a hard verification failure independent of mode, or require footer presence as a precondition before mode is consulted. | M |
| W3-03 | W3 | S2 | nonos-bootloader/src/kernel_verify/verify.rs:48 | `validate_image` parse failure on a footer-bearing image is downgraded to a raw-hash path that never verifies a signature. | verify.rs:48-56: `match validate_image(kernel_data) { Err(e) => { log_error; compute_and_display_hash(...); return result; } }` — on parse error it computes a hash but `signature_present` stays false (set false only implicitly via early `result` default) and `verify_and_display_signature` is skipped. A malformed footer thus degrades to "unsigned" rather than "rejected"; enforcement again rests entirely on `SecurityMode` (W3-01/W3-02). Live reach identical to W3-02. | On `validate_image` Err for an image that has a production footer, fail verification hard rather than falling back to raw-hash. | S |
| W3-04 | W3 | S2 | nonos-bootloader/src/boot/crypto/signature/error.rs:31 | Signature enforcement is a single runtime predicate (`mode.requires_signature()`); the no-signature and invalid-signature handlers both silently continue when it is false. | error.rs:25-47 `handle_no_signature`/`handle_invalid_signature`: the `else` arms (`!requires_signature()`) only `log_warn` + `update_stage(Success)` and return — no halt. This is the sole gate converting a failed/absent Ed25519 verification into a boot-or-halt decision; it is bypassable via W3-01. Recorded as the weakened-decision site that W3-01/02/03 funnel into. | Gate signature enforcement on a compile-time floor (e.g. `SecurityPolicy::from_build()`), not only the user-selectable runtime mode. | M |
| W3-05 | W3 | S2 | nonos-bootloader/src/boot/attestation/run/no_proof.rs:26 | ZK attestation is skipped when the kernel image carries no ZK proof and mode is `Development`; missing-proof acceptance is a single `mode == Development` predicate. | no_proof.rs:26 `if mode == SecurityMode::Development { ... return BootAttestationResult::default(); }`. Live reach: pipeline.rs:37 `run_zk_attestation` → orchestrate.rs:31 `if !has_zk_proof(data) { return handle_no_proof(...) }`. Non-Development modes `fatal_reset` (no_proof.rs:36) — fail-closed — but Development (reachable via W3-01) skips ZK silently. `failed.rs:26` (handle_verification_failed) has the identical Development bypass for a present-but-invalid proof. | Same as W3-04: bind ZK-required to a compile-time floor so a production build cannot reach the Development skip. | M |
| W3-06 | W3 | S3 | nonos-bootloader/src/boot/elf/extract.rs:25 | Kernel image footer is re-parsed at ELF-extract time instead of threading the already-verified `ParsedImage`/`kernel_bytes` from the crypto step; verified slice is not the one loaded. | `verify_kernel_crypto` (verify.rs:48) parses & verifies `parsed.kernel_bytes`; the result discards `parsed`. Later `run_elf_parse` (boot/elf/parse.rs:32) calls `extract_kernel_payload` (extract.rs:23-25) which calls `parse_image_footer(data)` AGAIN to re-derive `kernel_bytes`, then `load_kernel`. Not a classic disk-reread TOCTOU: `load_kernel_binary` (loader/file/load.rs:71, read.rs:85) reads the file exactly once into an owned immutable `Vec<u8>` that is passed by reference throughout; `parse_image_footer` is a pure deterministic function over that immutable buffer, so the re-derived slice is byte-identical to the verified one. Risk is parser-differential / defense-in-depth (two parse sites can drift independently), not an exploitable window in current code. | Thread the verified `ParsedImage` (or its `kernel_bytes` range) from the crypto step into ELF load; do not re-parse. | M |
| W3-07 | W3 | S4 | nonos-bootloader/src/zk/binding/replay/nonce.rs:32 | `get_boot_nonce()` silently returns all-zeros if the boot nonce was never initialized — a latent replay-protection footgun, but unreachable on the live spine. | nonce.rs:32 `BOOT_NONCE.lock().unwrap_or([0u8; 32])`. Live reach of caller: `build_public_inputs` (public_inputs.rs:60). Upstream, boot.rs:34 `initialize_zk_replay_protection` → zk_init/init.rs:27 `init_zk_nonce_required` (which `loop { spin_loop() }` secure-halts on entropy failure, zk_init/nonce.rs:35) and zk_init/init.rs:37-39 `verify_initialization()` secure-halts if `!is_nonce_initialized()`. Therefore by the time `get_boot_nonce()` runs, the nonce is provably initialized and the `[0u8;32]` arm is dead. Footgun only if future refactor removes the upstream halt. | Replace `get_boot_nonce()` call sites with `get_boot_nonce_checked()` and propagate `Err`, deleting the all-zeros default. | S |
| W3-08 | W3 | S4 | nonos-bootloader/Cargo.toml:96 | `mock-proof = []` feature is declared but referenced by zero `cfg` in `nonos-bootloader/src` and `build.rs`; it is dead and cannot weaken any path. | `grep -rn 'mock-proof\|mock_proof' nonos-bootloader/src nonos-bootloader/build.rs` → no `cfg(feature="mock-proof")` and no `mock`/`Mock` symbols anywhere. The flag gates no code; enabling it (even transitively) changes nothing. Recorded to close the proof obligation (a)/(d): there is no `mock-proof` acceptance path to reach. | Delete the unused `mock-proof` feature from Cargo.toml to remove a misleading trust-relevant knob. | S |
| W3-09 | W3 | S3 | nonos-bootloader/src/security/enforce/policy/types.rs:25 | Compile-time security policy floor falls back to `Standard` (release) / `Development` (debug_assertions) when neither `standard` nor `hardened` is set — a default-features build (`default` has no `standard`/`hardened`) yields only `Standard` policy. | types.rs:22-25: `#[cfg(feature="hardened")] return Hardened; #[cfg(feature="standard")] return Standard; #[cfg(not(any(...)))] { if cfg!(debug_assertions) Development else Standard }`. `default = ["logging","zk-groth16","zk-vk-provisioned","zk-zeroize"]` enables NEITHER `standard` nor `hardened`, so a plain `cargo build` (debug) → `SecurityPolicy::Development` (only `warn()`s, see modes/development.rs). This is the compile-time platform-policy floor (secure-boot/TPM/keys), separate from kernel-image signature gating; relevant to W5/W6 but recorded here because it is the cfg-resolution that the feature-gate proof depends on. | Make the no-feature default `Standard` even under `debug_assertions`, or require an explicit profile feature; treat absent profile as Hardened for release artifacts. | S |
| W3-10 | W3 | S4 | nonos-bootloader/src/zk/registry/validate.rs:21 | Build-time `const _: ()` `panic!` enforcing VK data ≥ 96 bytes is correct fail-to-compile behavior, gated to `all(zk-groth16, zk-vk-provisioned)` (the default set); it is NOT a runtime-reachable panic. | validate.rs:20-21 `#[cfg(all(feature="zk-groth16", feature="zk-vk-provisioned"))] const _: () = if VK_ALL_BYTES.len() < 96 { panic!(...) };`. Const-eval assertion → compile failure, never executes at boot. Implication for proof: a `--no-default-features --features hardened-production` build drops `zk-vk-provisioned`, removing this guard (see UNVERIFIED-1). | None for the assertion itself; track the build-profile VK-provisioning implication under UNVERIFIED-1. | — |
| W3-11 | W3 | S4 | nonos-bootloader/src/boot/security/policy.rs:37 | Secure Boot chain verification failure is downgraded to a `log_warn` and boot continues even when `secure_boot_enabled` is true. | policy.rs:36-40 `verify_chain`: `if security.secure_boot_enabled && !verify_secure_boot_chain(...) { log_warn("Secure Boot chain verification warning"); }` — no halt, return ignored. Live reach: run.rs:36 `verify_chain` ← `run_security_checks` ← boot.rs:33. This weakens the Secure Boot guarantee but is platform-attestation policy (W5/W6 scope); recorded here as a swallowed security-step result observed on the live spine. | Escalate a failed Secure Boot chain to `fatal_reset` under Hardened/production policy. | S |
| W3-12 | W3 | S4 | nonos-bootloader/src/entry/dev.rs:36 | `dev_override` is the only `dev-mode`-gated code in the bootloader and is correctly compiled out (`=> false`) when `dev-mode` is absent — confirms the F12 dev path is excluded under production. | dev.rs:19-49: the F12/SecureBoot-check `dev_override` is `#[cfg(feature="dev-mode")]`; dev.rs:35-38 `#[cfg(not(feature="dev-mode"))] pub fn dev_override(_) -> bool { false }`. `production=["hardened"]→["standard"]`; no path enables `dev-mode`. So the F12 bypass is genuinely excluded (not merely named) under production. Recorded as the positive cfg-trace evidence for proof (d); the residual production bypass is the menu entry, not F12 (see W3-01). | None — informational; confirms (d) for the F12 path. | — |

## Feature-gate proof (cfg-trace verdicts)

Feature graph (Cargo.toml): `default=["logging","zk-groth16","zk-vk-provisioned","zk-zeroize"]`, `hardened=["standard"]`, `production=["hardened"]`, `hardened-production=["production"]`, `dev-qemu=["dev-mode"]`, `zk-zeroize=["zeroize"]`, `zk-snark=["zk-groth16"]`. `standard=[]`, `dev-mode=[]`, `mock-proof=[]`, `zk-vk-provisioned=[]` are leaf flags. The only `dev-mode` cfg is entry/dev.rs; the only `mock-proof` references are the Cargo.toml declaration (zero source `cfg`).

- **(a) `production`/`hardened-production` cannot transitively enable `mock-proof`.** PROVEN-safe. Transitive closure of `hardened-production → production → hardened → standard` adds only `standard`; none of `hardened`, `production`, `hardened-production`, `standard` lists `mock-proof`. Additionally `mock-proof` gates zero code (W3-08), so even an explicit `--features mock-proof` is inert. Verdict: PROVEN-safe.
- **(b) They cannot skip signature verification.** PROVEN-safe at the compile-feature level, with an S1 runtime caveat. No `cfg(feature=...)` removes or short-circuits `verify_signature_bytes` / the `verify_signature` enforcement; signature enforcement is governed by the runtime `SecurityMode::requires_signature()` (mode.rs:28), not any feature. A `production` build still compiles the full Ed25519 path. CAVEAT (NOT a feature-gate violation, but in-scope per task wording "any path where a production build can weaken/skip a trust check is S1"): the ungated `Development` menu entry (W3-01) lets a console attacker pick a mode whose `requires_signature()` is false on a production binary. Verdict: PROVEN-safe (no cfg skips sig) + S1 runtime bypass W3-01.
- **(c) They cannot skip ZK verification.** PROVEN-safe at the compile-feature level, fail-closed when `zk-groth16` is absent. With `zk-groth16` (in `default`, and pulled by `zk-snark`) the Groth16 backend runs (zk/verify/core.rs:55-69). Under `--no-default-features --features hardened-production` (no `zk-groth16`), `verify_backend` is the `#[cfg(not(feature="zk-groth16"))]` arm (core.rs:71-74) returning `ZkVerifyResult::Unsupported` → `build_result` sets `zk_verified=false` (zk/attest/verify/boot.rs:49) → orchestrate.rs:33 `handle_verification_failed` → `fatal_reset` for non-Development. So a ZK-proof-bearing kernel CANNOT pass without the backend; ZK is fail-closed. The only skip is Development (W3-05), already counted in W3-01. Verdict: PROVEN-safe (fail-closed); residual skip = W3-01/W3-05.
- **(d) `dev-mode`/`mock-proof` code paths are actually excluded under production (not merely named).** PROVEN for `dev-mode` F12 path and `mock-proof`; S1 residual for the menu. `dev-mode`: only consumer is `dev_override`; `#[cfg(not(feature="dev-mode"))]` stub returns `false` (entry/dev.rs:35-38, W3-12) so F12 is excluded. `mock-proof`: no consumers (W3-08). HOWEVER `SecurityMode::Development` itself is NOT `dev-mode`-gated — it is an ordinary enum variant offered unconditionally in `DEFAULT_ENTRIES` (W3-01). So "dev paths excluded" holds for the feature-gated ones but the Development *mode* leaks into production via the menu. Verdict: PROVEN for feature-gated dev/mock code; **S1 VIOLATION of the intent** via ungated menu entry (W3-01).

## UNVERIFIED (W3)

- **UNVERIFIED-1 — VK-provisioning under non-default production profiles.** `zk-vk-provisioned` is in `default` but NOT pulled by `production`/`hardened-production` (Cargo.toml). A release artifact built `cargo build --release --no-default-features --features hardened-production` would compile WITHOUT `zk-groth16` and WITHOUT `zk-vk-provisioned`, so (i) the build-time VK-size assertion (validate.rs:21, W3-10) is dropped and (ii) ZK is the fail-closed `Unsupported` stub (core.rs:71). Whether production is *intended* to be built with `--no-default-features` or with `default` features ON (keeping `zk-groth16`+`zk-vk-provisioned`) is indeterminate from source alone. Settling experiment: inspect the release build invocation (CI/Makefile/`xtask`/packaging script) — `grep -rn 'no-default-features\|--features' Makefile* xtask* .github/ scripts/ 2>/dev/null` — and/or `cargo build --release --no-default-features --features hardened-production` then `nm -C target/.../nonos_boot | grep -i groth16` to confirm whether the Groth16 verifier symbol is present in the shipped binary. If production is built with default features retained, (c)/UNVERIFIED-1 are fully PROVEN-safe; if built `--no-default-features`, production ships a ZK-stub binary (still fail-closed for proof-bearing kernels, but no real ZK verification capability) — escalate to S1/S2 in Task 8.
- **UNVERIFIED-2 — `cfg!(debug_assertions)` resolution for shipped artifacts (W3-09).** `SecurityPolicy::from_build()` returns `Development` only under `debug_assertions` AND no `standard`/`hardened`. Release profile disables `debug_assertions`, so a release no-profile build yields `Standard`, not `Development`. This is determinate by Rust profile semantics, but the *shipped* profile (release vs debug) for production images is a build-system fact not in source. Settling experiment: confirm the release packaging uses `--release` (or a profile with `debug-assertions = false`) via the same CI/Makefile inspection as UNVERIFIED-1.

## W4 — Optimization audit (redundant hashing/copying, post-EBS alloc, re-enumeration, blocking delays)

W4 scopes the LIVE boot spine only (same spine as W3; dead trees `tpm/`,`verify/`,`security/verify/`,`crypto/keyring/`,`crypto/keystore_v2/` excluded). Cost-only/cleanliness = S4; post-EBS / no-reclaim-window hazards ≥ S2. Findings are diagnostic; no code modified. Redundant-hashing verdict: the kernel image is BLAKE3-hashed exactly **once** on the live spine (`kernel_verify/hash.rs:32`); Ed25519's internal SHA-512 over the same bytes (`crypto/verify/bytes.rs:33`) is inherent to the scheme, not a removable redundancy; `loader/security/hash.rs` blake3 is dead (tests/re-export only) so there is no second full-image BLAKE3. The headline cost is therefore not double-hashing but a redundant full-image **memcpy + page allocation** in the file reader (W4-01). Post-EBS allocation verdict: **none** — the path after `st.exit_boot_services()` (orchestrate.rs:87) does zero allocations; `copy_memory_map` writes into the pre-EBS-allocated raw `mmap_addr` (W4-08, recorded as a clean negative).

| ID | WS | Severity | path:line | Claim | Evidence | Recommended fix | Effort |
|----|----|----------|-----------|-------|----------|-----------------|--------|
| W4-01 | W4 | S4 | nonos-bootloader/src/loader/file/read.rs:85 | The kernel image is copied an extra full time in RAM: after the file is read into a UEFI page allocation it is `extend_from_slice`-copied into a fresh `Vec`, then the pages are freed — one redundant whole-image memcpy plus one redundant page allocation of `ceil(file_size/4096)` pages, on the boot critical path. | read.rs:51-63 allocates `pages` LOADER_DATA pages and forms the `buffer` slice over them (`:51-61` `allocate_pages`, `:63` `from_raw_parts_mut`); read.rs:65-83 is the actual file-read loop (`while total_read < file_size { file.read(&mut buffer[total_read..]) ... }`) that reads the whole file into `buffer` (the necessary disk read, pass 1). read.rs:85-86 `let mut result = Vec::with_capacity(file_size); result.extend_from_slice(buffer);` copies all `file_size` bytes a second time into a heap `Vec`; read.rs:88 `bs.free_pages(buffer_addr, pages)` then frees the first buffer. Live reach: `run_kernel_load` (boot/kernel/load.rs:30) → `load_kernel_binary` (loader/file/load.rs:71) → `read_regular_file` (load.rs:53). `data` is then used immutably everywhere (W3-06). Net waste = one extra `memcpy(file_size)` + a transient double residency of the image (UEFI pages + Vec) + alloc/free churn; bytes moved = image size (a multi-MiB kernel.bin, UNVERIFIED-W4-1). The owned `Vec` itself is required (caller needs `Vec<u8>`); the redundancy is reading into UEFI pages first instead of straight into the `Vec`. | Read directly into a `Vec`/`Box<[u8]>` (size known from `FileInfo`), or hand the UEFI page allocation to the caller as the owned image, eliminating the second copy and the alloc/free pair. | S |
| W4-02 | W4 | S4 | nonos-bootloader/src/crypto/verify/bytes.rs:33 | The full kernel image is digested twice by *different* primitives on the live spine — once by BLAKE3 for the measurement (`kernel_verify/hash.rs:32`) and once by Ed25519's internal SHA-512 inside `pk.verify(data,&sig)` over the same `kernel_code` slice — but the SHA-512 pass is inherent to Ed25519 and not removable; recorded so it is not mistaken for a fixable double-hash. | `compute_and_display_hash` (kernel_verify/hash.rs:32 `blake3::hash(kernel_code)`) hashes `parsed.kernel_bytes` once; `verify_and_display_signature` (kernel_verify/signature.rs:43) then calls `verify_signature_bytes` (crypto/verify/bytes.rs:23) whose loop runs `pk.verify(data,&sig)` (bytes.rs:33) over the *same* `kernel_code` — Ed25519 internally SHA-512s the whole message. Two full-image passes total, but distinct algorithms serving distinct purposes (measurement vs signature); neither is the *same* digest computed twice. Worst case the verify loop re-runs `pk.verify` once per trusted key (bytes.rs:29 `for i in 0..store.count`) → up to `store.count` SHA-512 passes over the whole image before a match/exhaustion. | None for the BLAKE3+Ed25519 split (correct by design); optionally cap/short-circuit the per-key `pk.verify` loop by keying on the footer's declared signer key-id so only one Ed25519 pass over the image runs. | S |
| W4-03 | W4 | S4 | nonos-bootloader/src/handoff/jump/settle.rs:17 | An unconditional fixed `SETTLE_ITERATIONS = 5_000_000`-iteration `spin_loop` (`settle_delay`) sits on the critical path immediately before `ExitBootServices`, adding a fixed boot-latency stall with no functional dependency, regardless of `gop`/mode. | settle.rs:17 `const SETTLE_ITERATIONS: usize = 5_000_000;`; settle.rs:21-25 `settle_delay` loops `core::hint::spin_loop()` that many times. Called unconditionally at handoff/exit/orchestrate.rs:85 `settle_delay();` (between `secure_cleanup_before_jump()` and `st.exit_boot_services()` at orchestrate.rs:87). Wall-clock cost is CPU-frequency-dependent and not source-determinable (UNVERIFIED-W4-2) but is paid every boot with no branch. | Replace the fixed spin with a measured `stall(BootServices)`-based microsecond delay tied to the actual hardware-settle requirement, or delete if no device needs it. | S |
| W4-04 | W4 | S4 | nonos-bootloader/src/boot/prepare/run.rs:42 | A second unconditional fixed delay — `mini_delay()` = 8,000,000 `spin_loop` iterations — runs on the critical path just before `exit_and_jump`, stacked ahead of W4-03's 5M-iter settle; ~13M spin iterations of pure latency are added to every boot regardless of `gop`. | boot/util/delay.rs:17-18 `pub fn mini_delay() { for _ in 0..8_000_000 { core::hint::spin_loop(); } }`. boot/prepare/run.rs:42 calls `mini_delay();` unconditionally (it is *outside* the `if gop` guards at run.rs:37/41) right before run.rs:46 `exit_and_jump(...)`, which itself reaches W4-03's `settle_delay()`. Two back-to-back unconditional fixed delays on the last leg of boot; combined ≈ 13M spin iterations (wall time UNVERIFIED-W4-2). This is *this* `mini_delay` only (boot/util/delay.rs:17, 8M iters); the verify stage has a separately-named, far smaller `mini_delay` cluster tracked in W4-09 (kernel_verify/delay.rs:18, 128 iters/site) — see W4-09 for the full fixed-delay picture; the ≈13M figure here covers run.rs:42 + settle.rs only and is exact for that scope. | Drop the decorative `mini_delay()` at run.rs:42 (no device/timing dependency) or fold any genuine settle need into the single measured delay from W4-03. | S |
| W4-05 | W4 | S4 | nonos-bootloader/src/entry/boot.rs:35 | The entire `run_hardware_discovery` result is computed and immediately discarded (`let _hw = ...`), yet the one value handoff actually consumes (ACPI RSDP) is independently re-derived later by a second config-table walk — full hardware enumeration done for nothing plus a duplicated RSDP probe. | entry/boot.rs:35 `let _hw = run_hardware_discovery(&mut st, gop);` — bound to `_hw`, never used; `run_verified_boot` (boot.rs:36) takes no `HardwareInfo`. Inside, `discover_system_hardware` (hardware/discover.rs:28-43) does: `discover_acpi_rsdp` (config-table walk + RSDP checksum, rsdp.rs:20), `discover_memory_size`, `get_cpu_count_from_acpi`, four `bs.find_handles` protocol-DB walks (`enumerate_storage/network/graphics/pci`, devices/enumerate.rs:22-48), `detect_cpu_features` (CPUID, discover.rs:39) and several `alloc::format!` log strings — all dropped. Separately, the RSDP that handoff needs is recomputed at handoff/exit/gather.rs:31 `get_acpi_rsdp(st)` (handoff/config/acpi.rs:21), a *second* independent walk of the same `st.config_table()` for the same ACPI2/ACPI GUID. So the live spine walks the UEFI config table for the RSDP twice and runs a full device enumeration whose result has zero consumers. | Either consume `HardwareInfo` (thread `hw.rsdp_address` into the handoff path so `get_acpi_rsdp` is not re-walked) or delete the discarded `run_hardware_discovery` call; do not enumerate-then-drop. | M |
| W4-06 | W4 | S4 | nonos-bootloader/src/handoff/exit/gather.rs:30 | CPU security features (SMEP/SMAP/UMIP via CPUID leaf 7) are detected three times on the live spine — in `run_security_checks`, in the discarded `run_hardware_discovery`, and again at handoff — recomputing the same CPUID-derived bits instead of threading the first result. | (1) `run_security_checks` → `verify_hardware_requirements` (boot/security/hardware.rs:26 `detect_hardware_capabilities()`) → `detect_cpu_security_features` (security/hardware/cpu/detect.rs:21, CPUID leaves 7/1/ext). (2) Discarded `run_hardware_discovery` → `detect_cpu_features` (hardware/cpu/features.rs:22, CPUID 0x80000001 + leaf-7-sub-0) at hardware/discover.rs:39 (result dropped per W4-05). (3) handoff/exit/gather.rs:30 `let (smep, smap, umip) = detect_cpu_security_features();` re-invokes the same security/hardware/cpu/detect.rs:21 function at handoff time. CPUID is a single uncached instruction (no MMIO, ~cheap) so the cycle cost is small; the cost is duplicated logic across three call sites that can drift (two different feature structs decode the *same* leaf-7 bits) rather than wall-clock. | Detect CPU features once early, store in `SecurityContext`/handoff params, and read that struct at gather time; collapse `detect_cpu_features`/`detect_cpu_security_features` to one decoder. | M |
| W4-07 | W4 | S4 | nonos-bootloader/src/boot/crypto/hash.rs:42 | `animate_hash_display` spins `32 × micro_delay()` (32 × 1,500,000 = 48,000,000 `spin_loop` iterations) purely for a hash-reveal animation on the crypto-verify critical path; gated on `gop` so it only fires when a display is attached, but when it does it is the single largest *single* fixed stall on the live spine — larger than W4-04's 8M `mini_delay` and W4-03's 5M `settle_delay`, and far larger than the W4-09 `kernel_verify` `mini_delay` cluster (a *different* 128-iter function, ~896 iters total, not the 8M one); 48M remains both the largest single stall and the dominant aggregate. | hash.rs:34 `if gop { animate_hash_display(state); }`; hash.rs:41-43 `fn animate_hash_display(...) { for _ in 0..32 { animate_hash_reveal(); show_crypto_verification(state); micro_delay(); } }`; `micro_delay` = 1,500,000 spin iters (boot/util/delay.rs:21-22). Conditional (only with framebuffer) but unconditional within that branch and on the verify path before ELF load. Wall time UNVERIFIED-W4-2. Distinct from W4-03/04 (those are unconditional). | Reduce the iteration count / `micro_delay` magnitude or make the reveal frame-rate driven rather than a fixed 48M-iteration busy-spin. | S |
| W4-08 | W4 | S4 | nonos-bootloader/src/handoff/exit/orchestrate.rs:87 | (Clean negative, recorded for the audit obligation) No allocation occurs after `ExitBootServices` or in the no-reclaim window on the live spine; the post-EBS memory-map copy writes into a buffer allocated *before* EBS. | orchestrate.rs:42 `allocate_handoff_resources(&st, cmdline)` and orchestrate.rs:60 `build_kernel_pml4(bs, kernel)` run while Boot Services are live; orchestrate.rs:84 `secure_cleanup_before_jump()` (no alloc — pure wipes, cleanup.rs:20-42) then orchestrate.rs:87 `st.exit_boot_services()`. Everything after: orchestrate.rs:89 `copy_memory_map(allocs.mmap_addr, &final_mmap)` (jump/mmap.rs:23-38 writes raw into the pre-EBS `mmap_addr`, no `alloc`/`Vec`/`Box`/pool), `finalize_mmap` (raw pointer store), `switch_to_kernel_pml4`, `validate_and_jump`→`jump_to_kernel` (handoff/jump/entry.rs:27, no alloc). `grep` of `handoff/exit/`+`handoff/jump/` for `alloc::`/`Vec::`/`Box::`/`format!`/`allocate_pool`/`allocate_pages` after line 87 = none. No post-EBS hazard. | None — invariant holds; recommend an explicit `#![deny]`/lint or a comment pinning "no alloc past orchestrate.rs:87" to prevent regression. | — |
| W4-09 | W4 | S4 | nonos-bootloader/src/kernel_verify/delay.rs:18 | A cluster of decorative fixed `mini_delay()` calls is sprinkled through the `kernel_verify` stage of the live signed-boot spine (verify/hash/signature), one after each `[CRYPTO]` status print. Note: this is a **second, distinct** `mini_delay` from W4-04's — kernel_verify/delay.rs:18-22 is `for _ in 0..128`, *not* boot/util/delay.rs's `0..8_000_000`. The aggregate is therefore tiny (~7×128 ≈ 896 spin iters), but it is unconditional on the live spine and is pure presentation latency with zero functional dependency. | kernel_verify/delay.rs:18-22 `pub fn mini_delay() { for _ in 0..128 { core::hint::spin_loop(); } }` (128 iters/site, distinct from boot/util/delay.rs:17). Live signed spine: `run_crypto_verification` (boot/crypto/run.rs:30) → `compute_hash` (boot/crypto/hash.rs:27) → `verify_kernel_crypto` (kernel_verify/verify.rs:26, called *unconditionally* at boot/crypto/hash.rs:30, no `gop` guard). For a SIGNED production kernel (`has_production_footer` true at verify.rs:40 + `validate_image` Ok at verify.rs:48): **(1)** verify.rs:62 `mini_delay()` after `print_kernel_size` — unconditional on signed branch. **(2)** verify.rs:64 → `compute_and_display_hash` → hash.rs:31 `mini_delay()` and **(3)** hash.rs:40 `mini_delay()` — both unconditional. **(4)** verify.rs:65 → `verify_and_display_signature` → signature.rs:34 `mini_delay()` (before the all-zeros check) — unconditional. **(5)** signature.rs:41 → `display_signature_components` → signature.rs:72 `mini_delay()` — unconditional on signed spine. **(6)** signature.rs:52 `mini_delay()` at end of `verify_and_display_signature` — unconditional on signed spine (the early `return` at signature.rs:39 is the all-zeros/unsigned branch, not taken by a production signed kernel). **(7)** signature.rs:59 `mini_delay()` inside `signature_passed` — reached only on `Ok(key_id)` i.e. signature **valid**; honestly this one is *conditional on verification success*, which is the normal production outcome but not strictly unconditional. Confirmed-unconditional-on-signed-spine total = 6 sites × 128 = 768 iters; including the success-path site (7) = 896 iters. No site is `gop`/dev-gated or error-only. Wall time is negligible (UNVERIFIED-W4-2 not applicable — magnitude is ~3 orders smaller than W4-03/04/07). | Gate the decorative verify-stage `mini_delay()` calls behind a dev/verbose `cfg` (production signed boot needs none), or delete `kernel_verify/delay.rs` entirely and drop the `use super::delay::mini_delay;` imports — the magnitude is trivial so this is cleanliness/intent, not a latency fix. | XS |

## UNVERIFIED (W4)

- **UNVERIFIED-W4-1 — kernel image size class (drives W4-01 / W4-02 byte counts).** The redundant memcpy in W4-01 and the per-key Ed25519 passes in W4-02 cost bytes proportional to the built `kernel.bin`; the size is a build artifact not in source (`MAX_KERNEL_SIZE` in loader/file/types.rs is only the ceiling). Measuring experiment: `ls -l` the built image at `\EFI\nonos\kernel.bin` (or the loader output of `cargo build` for `nonos-kernel`) to fix the size class; W4-01 redundant copy = exactly that many bytes, W4-02 worst case = `size × KEYSTORE.count` SHA-512 bytes.
- **UNVERIFIED-W4-2 — wall-clock cost of the fixed spin delays (W4-03 / W4-04 / W4-07).** `settle_delay` (5M iters), `mini_delay` (8M iters), and `32×micro_delay` (48M iters) are iteration counts; wall time depends on CPU frequency and `spin_loop` (PAUSE) latency, not determinable from source. Measuring experiment: add COM1 timestamp markers (the codebase already has `com1_marker`, used at orchestrate.rs:59/64/86/88) immediately before and after each call site (`handoff/jump/settle.rs:21`, `boot/prepare/run.rs:42`, `boot/crypto/hash.rs:42`) and diff the serial-log timestamps on real boot to convert iteration counts to milliseconds of added latency.
