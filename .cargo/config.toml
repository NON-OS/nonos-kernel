[build]
target = "x86_64-nonos.json"

[target.x86_64-nonos]
rustflags = [
  "-C", "force-frame-pointers=yes",
]

# Platform-specific runners are defined in the Makefile for portability
# Use `make run` instead of `cargo run` for QEMU testing
